<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="ColorCircleProgressView">
        <!--定义圆环的渐变颜色-->
        <attr name="Color01" format="color"/>
        <attr name="Color02" format="color"/>
        <attr name="Color03" format="color"/>
        <attr name="Color04" format="color"/>
        <attr name="Color05" format="color"/>
        <attr name="Color06" format="color"/>
        <attr name="Color07" format="color"/>
        <!--定义圆环角度及开始位置的角度,圆环到view边框的距离-->
        <attr name="ViewAngle" format="integer"/>
        <attr name="StartAngle" format="integer"/>
        <attr name="ViewPadding" format="integer"/>
        <!--定义圆环的大小，是否圆角-->
        <attr name="StrokeWith" format="integer"/>
        <attr name="IsRound" format="boolean"/>

        <!--定义点的大小和颜色-->
        <attr name="PointColor" format="color"/>
        <attr name="PointRadio" format="integer"/>

    </declare-styleable>

    <!-- 是否开启抗锯齿 -->
    <attr name="antiAlias" format="boolean" />
    <!-- 圆弧起始角度，3点钟方向为0，顺时针递增，小于0或大于360进行取余 -->
    <attr name="startAngle" format="float" />
    <!-- 圆弧度数 -->
    <attr name="sweepAngle" format="float" />
    <!-- 设置动画时间 -->
    <attr name="animTime" format="integer" />
    <!-- 绘制内容的数值 -->
    <attr name="maxValue" format="float" />
    <attr name="value" format="float" />
    <!-- 绘制内容的单位 -->
    <attr name="unit" format="string|reference" />
    <attr name="unitSize" format="dimension" />
    <attr name="unitColor" format="color|reference" />
    <!-- 绘制内容相应的提示语 -->
    <attr name="hint" format="string|reference" />
    <attr name="hintSize" format="dimension" />
    <attr name="hintColor" format="color|reference" />
    <!-- 精度，默认为0 -->
    <attr name="precision" format="integer" />
    <attr name="valueSize" format="dimension" />
    <attr name="valueColor" format="color|reference" />
    <!-- 圆弧颜色，设置多个可实现渐变 -->
    <attr name="arcColor1" format="color|reference" />
    <attr name="arcColor2" format="color|reference" />
    <attr name="arcColor3" format="color|reference" />
    <!-- 背景圆弧颜色，默认白色 -->
    <attr name="bgArcColor" format="color|reference" />
    <!-- 圆弧宽度 -->
    <attr name="arcWidth" format="dimension" />
    <!-- 圆弧颜色， -->
    <attr name="arcColors" format="color|reference" />
    <!-- 文字的偏移量。相对于圆半径而言，默认三分之一 -->
    <attr name="textOffsetPercentInRadius" format="float" />

    <!-- 圆形进度条 -->
    <declare-styleable name="CircleProgressBar">
        <attr name="antiAlias" />
        <attr name="startAngle" />
        <attr name="sweepAngle" />
        <attr name="animTime" />
        <attr name="minValue" />
        <attr name="maxValue" />
        <attr name="minSelectValue" />
        <attr name="maxSelectValue" />
        <attr name="value" />
        <attr name="precision" />
        <attr name="valueSize" />
        <attr name="valueColor" />
        <attr name="textOffsetPercentInRadius" />
        <!-- 绘制内容相应的提示语 -->
        <attr name="hint" />
        <attr name="hintSize" />
        <attr name="hintColor" />
        <!-- 绘制内容的单位 -->
        <attr name="unit" />
        <attr name="unitSize" />
        <attr name="unitColor" />
        <!-- 圆弧宽度 -->
        <attr name="arcWidth" />
        <attr name="arcColors" />
        <!-- 背景圆弧颜色 -->
        <attr name="bgArcColor" />
        <!-- 背景圆弧宽度 -->
        <attr name="bgArcWidth" format="dimension" />
        <!-- 是否不进行Text的绘制 -->
        <attr name="noText" format="boolean" />
    </declare-styleable>

    <declare-styleable name="DialProgress">
        <attr name="antiAlias" />
        <attr name="startAngle" />
        <attr name="sweepAngle" />
        <attr name="animTime" />
        <attr name="maxValue" />
        <attr name="value" />
        <attr name="precision" />
        <attr name="valueSize" />
        <attr name="valueColor" />
        <attr name="textOffsetPercentInRadius" />
        <!-- 绘制内容的单位 -->
        <attr name="unit" />
        <attr name="unitSize" />
        <attr name="unitColor" />
        <!-- 绘制内容相应的提示语 -->
        <attr name="hint" />
        <attr name="hintSize" />
        <attr name="hintColor" />
        <!-- 圆弧的宽度 -->
        <attr name="arcWidth" />
        <!-- 刻度的宽度 -->
        <attr name="dialWidth" format="dimension|reference" />
        <!-- 刻度之间的间隔 -->
        <attr name="dialIntervalDegree" format="integer" />
        <!-- 圆弧颜色， -->
        <attr name="arcColors" />
        <!-- 背景圆弧线颜色 -->
        <attr name="bgArcColor" />
        <!-- 刻度线颜色 -->
        <attr name="dialColor" format="color|reference" />
    </declare-styleable>

    <declare-styleable name="WaveProgress">
        <!-- 是否开启抗锯齿 -->
        <attr name="antiAlias" />
        <!-- 深色水波动画时间 -->
        <attr name="darkWaveAnimTime" format="integer" />
        <!-- 浅色水波动画时间 -->
        <attr name="lightWaveAnimTime" format="integer" />
        <!-- 最大值 -->
        <attr name="maxValue" />
        <!-- 当前值 -->
        <attr name="value" />
        <attr name="valueColor" />
        <attr name="valueSize" />
        <!-- 绘制内容相应的提示语 -->
        <attr name="hint" />
        <attr name="hintSize" />
        <attr name="hintColor" />
        <!-- 圆环宽度 -->
        <attr name="circleWidth" format="dimension" />
        <!-- 圆环颜色 -->
        <attr name="circleColor" format="color|reference" />
        <!-- 背景圆环颜色 -->
        <attr name="bgCircleColor" format="color|reference" />
        <!-- 锁定水波不随圆环进度改变，默认锁定在50%处 -->
        <attr name="lockWave" format="boolean" />
        <!-- 水波数量 -->
        <attr name="waveNum" format="integer" />
        <!-- 水波高度，峰值和谷值之和 -->
        <attr name="waveHeight" format="dimension" />
        <!-- 深色水波颜色 -->
        <attr name="darkWaveColor" format="color|reference" />
        <!-- 是否显示浅色水波 -->
        <attr name="showLightWave" format="boolean" />
        <!-- 浅色水波颜色 -->
        <attr name="lightWaveColor" format="color|reference" />
        <!-- 浅色水波的方向 -->
        <attr name="lightWaveDirect" format="enum">
            <enum name="L2R" value="0" />
            <enum name="R2L" value="1" />
        </attr>
    </declare-styleable>

    <declare-styleable name="viewmodule_DigitalClockView">
        <attr name="viewmodule_digitalTimeTextSize" format="dimension" />
        <attr name="viewmodule_digitalTimeTextColor" format="color" />
        <attr name="viewmodule_digitalDateTextSize" format="dimension" />
        <attr name="viewmodule_digitalDateTextColor" format="color" />
    </declare-styleable>

    <declare-styleable name="DigitalClockViewEx">
        <attr name="TimeTextSize" format="dimension" />
        <attr name="TimeTextColor" format="color" />
        <attr name="DateTextSize" format="dimension" />
        <attr name="DateTextColor" format="color" />
    </declare-styleable>

    <declare-styleable name="viewmodule_PatternLockView">
        <attr name="viewmodule_dotCount" format="integer"/>
        <attr name="viewmodule_dotNormalSize" format="dimension"/>
        <attr name="viewmodule_dotSelectedSize" format="dimension"/>
        <attr name="viewmodule_pathWidth" format="dimension"/>
        <attr name="viewmodule_aspectRatioEnabled" format="boolean"/>
        <attr name="viewmodule_aspectRatio" format="enum">
            <enum name="square" value="0"/>
            <enum name="width_bias" value="1"/>
            <enum name="height_bias" value="2"/>
        </attr>
        <attr name="viewmodule_normalStateColor" format="color"/>
        <attr name="viewmodule_correctStateColor" format="color"/>
        <attr name="viewmodule_wrongStateColor" format="color"/>
        <attr name="viewmodule_dotAnimationDuration" format="integer"/>
        <attr name="viewmodule_pathEndAnimationDuration" format="integer"/>
        <attr name="viewmodule_ignored_dots" />
        <attr name="viewmodule_dot_offsets" />
    </declare-styleable>
    <declare-styleable name="CookerView">
        <attr name="TimerTextSize" format="float"/>
    </declare-styleable>
    <declare-styleable name="RectangleCookerView">
        <attr name="b_shape" format="enum">
            <enum name="top_rounded" value="0"/>
            <enum name="bottom_rounded" value="1"/>
        </attr>
    </declare-styleable>
    <declare-styleable name="QuarterOvalImageView">
        <attr name="vertex_position" format="enum">
            <enum name="top_left" value="0"/>
            <enum name="top_right" value="1"/>
            <enum name="bottom_left" value="2"/>
            <enum name="bottom_right" value="3"/>
        </attr>
    </declare-styleable>
    <declare-styleable name="ToggleButtonView">
        <attr name="image_on" format="integer"/>
        <attr name="image_off" format="integer"/>
        <attr name="checked" format="boolean"/>
    </declare-styleable>
</resources>